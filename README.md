# SHADOWBOUND

Android-ready 2D action-platformer prototype.

## Android APK
GitHub Actions builds a debug APK automatically on pushes to `main`, or manually from **Actions → Build SHADOWBOUND Android APK → Run workflow**.

The generated APK is uploaded as the `shadowbound-debug-apk` workflow artifact.

## Controls
Touch controls are included for Android. Gamepad and keyboard controls are also supported.
